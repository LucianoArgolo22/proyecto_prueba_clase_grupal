-- DDL: alquiler_amba.gold.dim_tiempo
-- Spec: gold/SPEC.md — SCD Tipo 0. BK = fecha.
-- NO EJECUTAR hasta Capa 3.

CREATE SCHEMA IF NOT EXISTS alquiler_amba.gold;

-- CREATE TABLE IF NOT EXISTS alquiler_amba.gold.dim_tiempo (
--   tiempo_sk BIGINT,
--   fecha DATE,
--   anio INT,
--   mes INT,
--   dia INT,
--   quincena_nro INT,
--   quincena_id STRING
-- );
