-- DDL: alquiler_amba.gold.dim_estado
-- Spec: gold/SPEC.md — SCD1. BK = estado_std.
-- NO EJECUTAR hasta Capa 3.

-- CREATE TABLE IF NOT EXISTS alquiler_amba.gold.dim_estado (
--   estado_sk BIGINT,
--   estado_std STRING,
--   estado_label STRING
-- );
