-- DDL: alquiler_amba.gold.dim_operacion_moneda
-- Spec: gold/SPEC.md — SCD1. BK = (operacion_std, moneda_std).
-- NO EJECUTAR hasta Capa 3. No hay dim_moneda ni dim_operacion sueltas.

-- CREATE TABLE IF NOT EXISTS alquiler_amba.gold.dim_operacion_moneda (
--   operacion_moneda_sk BIGINT,
--   operacion_std STRING,
--   moneda_std STRING,
--   es_alquiler BOOLEAN
-- );
